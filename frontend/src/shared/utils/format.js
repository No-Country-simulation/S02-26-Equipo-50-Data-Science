// format.js
// Re-exports from formatters.js for backwards compatibility and alias support

export { formatCurrency, formatDate, formatDateTime, formatPhone, formatRelativeDate, formatPercent } from './formatters';
